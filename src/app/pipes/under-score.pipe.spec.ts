import { UnderScorePipe } from './under-score.pipe';

describe('UnderScorePipe', () => {
  it('create an instance', () => {
    const pipe = new UnderScorePipe();
    expect(pipe).toBeTruthy();
  });
});
